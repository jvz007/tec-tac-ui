# Tec-Tac UI 0.12.26

## Signed managed hotfixes

- Managed Hotfix now accepts the hotfix ZIP together with detached `.sig` and `.release.json` signing companions.
- Shows verified publisher, key and package trust state before Apply.
- Explains when signature companions are missing and that production/development acceptance is controlled by the root-owned trust policy.
- Reuses the existing Modules trust presentation and interaction patterns.

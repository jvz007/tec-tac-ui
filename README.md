# Tec-Tac UI 0.4.0 Patch

Target repository: `/opt/tec-tac-ui` (`jvz007/tec-tac-ui`) only.

Requires Tec-Tac Framework 1.4.0 or newer within the 1.x series.

Install after Framework 1.4.0:

```bash
unzip tec-tac-ui-0.4.0-patch.zip
cd tec-tac-ui-0.4.0-patch
sudo bash apply.sh
```

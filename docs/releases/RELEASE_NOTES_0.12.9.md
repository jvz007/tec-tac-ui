# Tec-Tac UI 0.12.9

## Module reinstall confirmation

- changes the Online Module Catalog action to `Reinstall` when the installed and available versions match;
- shows a Core confirmation dialog before staging an identical module version;
- makes the same-version replacement intent explicit while retaining the existing package inspection and install confirmation workflow;
- keeps update and first-install behavior unchanged.

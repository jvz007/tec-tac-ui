# Tec-Tac UI 0.9.2

Corrective release for Public Contracts exports.

- Sends a neutral `Accept: */*` header for Markdown/Text attachment downloads.
- Continues to select the export type using `export_format=md|txt`.
- Requires Tec-Tac Framework `>=1.10.2,<2.0.0`.

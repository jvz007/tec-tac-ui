# UI Test extension

Reference-only module used to validate Tec-Tac 1.2.0 / UI 0.2.0 package inspection, installation, UI synchronization, runtime route registration, RBAC discovery, and removal.

It owns no database models and declares no Django apps.
